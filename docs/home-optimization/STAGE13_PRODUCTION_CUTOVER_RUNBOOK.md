# 13단계 메인페이지 외부 자산 운영 전환·즉시 복구 실행서

이 문서는 골프조인 고객용 메인페이지를 경량 HTML로 전환할 때 사용하는 단일 실행서다. 위에서 아래 순서로 진행하며, 필수 항목 하나라도 실패하면 전환하지 않는다.

## 0. 현재 고정 상태

- [x] 현재 정상 단일 HTML: `EC5C6E23...`, 2,874,808 bytes
- [x] 운영 전환 경량 HTML: `F1DF1335...`, 267,744 bytes
- [x] 외부 CSS: `1499DBDB...`, 853,185 bytes
- [x] 외부 JavaScript: `12184E1A...`, 1,756,777 bytes
- [x] 자산 revision: `gha_c27406cf81e361a28044fc29`
- [x] eventPlanSeq 18 실제 PC·MO 기능과 재방문 캐시 검증 완료
- [x] 복구 목표 시간: 장애 판단 후 5분 이내
- [x] Product Discovery Gate는 현재 ON이며 이번 HTML 전환에서 변경하지 않는다.

고정 패키지:

```text
deploy/stage13-home-assets/production-cutover-20260813/
```

## 1. 파일 구분

### 운영 전환 파일

```text
DEPLOY_golfjoin_main_external_F1DF1335.html
```

이 파일만 고객용 골프조인 메인 이벤트 페이지에 붙여 넣는다.

### 즉시 복구 파일

```text
ROLLBACK_golfjoin_main_legacy_EC5C6E23.html
```

외부 CSS·JS 장애, 빈 화면, 스크롤 불가 또는 핵심 기능 장애가 발생하면 이 파일 전체 내용으로 되돌린다. 기존 단일 HTML이므로 GCS 외부 자산에 의존하지 않는다.

### 외부 자산 재업로드 원본

```text
ASSET_golfjoin-main_1499DBDB.css
ASSET_golfjoin-main_12184E1A.js
```

정상 전환에서는 다시 업로드하지 않는다. 원격 객체가 사라졌을 때 동일 불변 경로로 복구하기 위한 정확한 원본이다.

## 2. 전환 전 자동검사

- [ ] 아래 로컬 검사가 `ok: true`인지 확인한다.

```powershell
cd D:\secrettour_join\260521\golfjoin
npm.cmd run home:assets:verify-production
```

- [ ] 아래 원격 포함 검사가 `ok: true`인지 확인한다.

```powershell
cd D:\secrettour_join\260521\golfjoin
npm.cmd run home:assets:verify-production -- --remote
```

검사 내용은 배포·복구·CSS·JS 크기와 SHA-256, HTML 안의 URL·SRI, 복구 안내 기본 숨김, PC·MO CORS, MIME, immutable 캐시다.

## 3. 전환 중 변경 금지 항목

- [ ] Cloud Function을 함께 배포하지 않는다.
- [ ] 상품업데이트·상품군 업데이트를 함께 실행하지 않는다.
- [ ] Product Discovery 또는 Release V2 Gate를 변경하지 않는다.
- [ ] GCS의 기존 불변 CSS·JS를 덮어쓰거나 삭제하지 않는다.
- [ ] 다른 메인페이지 UI 수정과 동시에 배포하지 않는다.

이 단계에서는 HTML의 큰 CSS·JS를 외부 파일 참조로 바꾸는 작업만 한다. 데이터와 서버를 함께 바꾸지 않아 원인과 복구 범위를 하나로 제한한다.

## 4. 고객용 운영 전환

- [ ] 작업 시작 시각을 기록한다.
- [ ] 고객용 골프조인 메인 이벤트 페이지 `eventPlanSeq=3`의 HTML 편집 화면을 연다.
- [ ] 현재 입력된 HTML을 별도로 복사하지 않고도 고정 복구본 `EC5C6E23...`이 준비돼 있는지 다시 확인한다.
- [ ] 현재 HTML 전체를 제거한다.
- [ ] `DEPLOY_golfjoin_main_external_F1DF1335.html` 전체 내용을 붙여 넣는다.
- [ ] 저장한다.
- [ ] 저장 직후 다른 수정 없이 PC 비로그인 검증으로 이동한다.

## 5. 저장 직후 3분 필수검사

### PC 비로그인

- [ ] 일반 새로고침으로 메인페이지가 표시된다.
- [ ] 복구 안내 화면이 표시되지 않는다.
- [ ] 세로 스크롤이 된다.
- [ ] MD PICK과 취향맞춤 대표이미지가 표시된다.
- [ ] MD PICK 상품군 카드를 눌러 상세 모달이 열린다.
- [ ] 여행기간 카드와 가격·일정이 표시된다.
- [ ] 모달을 닫으면 기존 스크롤 위치로 돌아온다.
- [ ] Network에서 외부 CSS·JS가 각각 1건이며 200 또는 캐시 응답이다.
- [ ] 콘솔에 CSS·JS, SRI, CORS 또는 함수 미정의 오류가 없다.

### 모바일 비로그인

- [ ] 메인페이지가 가로로 밀리지 않고 세로 스크롤된다.
- [ ] MD PICK 카드가 정상 위치에 표시된다.
- [ ] 상품상세가 전체화면으로 열린다.
- [ ] 상품군 여행기간을 변경할 수 있다.
- [ ] 뒤로가기로 닫으면 기존 메인 위치가 복원된다.

### 로그인 회원

- [ ] 나의 모임 섹션의 해당 회원 일정만 표시된다.
- [ ] 내예약의 내가 만든 일정·참여중 카드와 참여자 아이콘이 표시된다.
- [ ] 상품상세의 참여자·현재인원·성별구성이 기존 운영 상태와 같다.
- [ ] 최근 본 상품·찜한상품·내예약 모달을 열고 닫을 수 있다.

## 6. 즉시 복구 판정

다음 중 하나라도 발생하면 추가 분석보다 복구를 먼저 실행한다.

- [ ] `페이지 구성 파일을 불러오지 못했습니다` 안내가 재시도 후에도 표시된다.
- [ ] 외부 CSS 또는 JavaScript가 4xx·5xx, CORS, MIME, SRI 오류로 실패한다.
- [ ] 메인 영역이 비거나 세로 스크롤이 잠긴다.
- [ ] PC 또는 모바일 상품카드가 상품상세를 열지 못한다.
- [ ] 모달을 닫아도 메인 위치로 돌아오지 못하거나 배경이 사라진다.
- [ ] 로그인 회원의 나의 모임·내예약이 전환 전과 다르게 깨진다.
- [ ] 원인을 2분 안에 단순 캐시 문제로 확정할 수 없다.

## 7. 5분 즉시 복구 절차

- [ ] 장애 발견 시각과 PC/MO·로그인 여부를 짧게 기록한다.
- [ ] 운영 이벤트 HTML 편집 화면을 연다.
- [ ] 경량 HTML 전체를 제거한다.
- [ ] `ROLLBACK_golfjoin_main_legacy_EC5C6E23.html` 전체 내용을 붙여 넣는다.
- [ ] 저장한다.
- [ ] 새 쿼리 파라미터를 붙이거나 새 시크릿 창에서 PC·MO를 확인한다.
- [ ] Network에 `golfjoin-main.css`, `golfjoin-main.js` 요청이 더 이상 없는지 확인한다.
- [ ] 메인 스크롤, 상품상세 열기·닫기, 로그인 나의 모임을 확인한다.
- [ ] 정상화 시각과 총 복구 시간을 기록한다.

복구 시 Cloud Function 재배포, GCS 삭제, 상품업데이트, Gate 변경은 하지 않는다. 이 전환의 복구는 HTML 한 파일 교체만으로 끝내야 한다.

## 8. 운영 성공 판정

- [ ] PC·MO 비로그인 필수검사를 모두 통과했다.
- [ ] 로그인 회원 필수검사를 모두 통과했다.
- [ ] 외부 자산 오류와 JavaScript 치명 오류가 0건이다.
- [ ] 일반 재방문에서 동일 CSS·JS가 브라우저 캐시로 제공된다.
- [ ] 전환 후 10분 동안 핵심 화면에서 새 장애가 없다.
- [ ] 완료 시각과 배포 해시 `F1DF1335`를 기록했다.

위 항목을 모두 통과한 뒤에만 운영 전환 완료로 기록한다. 외부 자산의 세부 모듈 분리와 critical CSS 축소는 별도 후속 작업이며 이번 전환에 섞지 않는다.
