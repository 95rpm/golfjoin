# `8A853...` 모바일 로그인 콜드 HAR run03

측정일: 2026-08-06 KST  
운영 URL: `https://m.secret-tour.com/event/plan_view?eventPlanSeq=3&page=1`  
환경: Chrome Device Toolbar 모바일 UA, 약 412×915, 로그인, `Disable cache`, `No throttling`  
운영 소스 경계: `8A853CA11B9DED569067D2E45317B883E44452868E7FA8E01A2CCFAD6A17ACCC`

## 1. 결론

- [x] 같은 운영 소스·브라우저 조건의 공식 세 번째 표본으로 채택한다.
- [x] 요청 122개, 전송량 5.68MiB, 전체 네트워크 기록 17.79초다.
- [!] 홈 카드 JSON은 1.93초에 준비됐지만 대표이미지 첫 요청은 9.75초로 7.81초 늦었다.
- [x] 대표이미지 13개는 요청 시작 뒤 최대 261ms 안에 내려받았다.
- [!] 상품 `goodSeq=30001242` 이미지는 다시 별도 단계에서 가장 늦게 요청됐다.
- [x] `home_bootstrap_light`는 한 번만 실행됐다.
- [x] 일정 API 뒤 참여 API는 이번에는 57ms 만에 시작됐다.
- [!] 외부 Supabase 상품 이미지는 세 번째로 status 0·blocked 상태였다.
- [x] 404·5xx 응답은 없었다.

## 2. 페이지 요약

| 항목 | run03 |
|---|---:|
| 요청 수 | 122개 |
| 전송량 | 5,957,343 bytes, 5.68MiB |
| 전체 네트워크 기록 | 17,787ms |
| DOMContentLoaded | 1,870ms |
| Load | 9,873ms |
| HTTP 200 | 116개 |
| HTTP 204 | 5개 |
| status 0 | 1개 |

run01·run02보다 요청이 적은 이유는 골프조인 배경·국기 이미지의 이중 요청이 이번 실행에서는 발생하지 않았고 슬라이드 이미지 구성도 달랐기 때문이다.

## 3. 정적 데이터와 대표이미지

| 요청 | 시작 | 종료 |
|---|---:|---:|
| 상품군 manifest | 1,725ms | 1,826ms |
| 홈 manifest | 1,725ms | 1,830ms |
| 상품군 catalog | 1,913ms | 1,930ms |
| 홈 카드 JSON | 1,913ms | 1,932ms |
| 대표이미지 첫 묶음 | 9,747ms | 10,008ms |
| 마지막 상품 이미지 | 17,771ms | 17,787ms |

홈 카드 JSON 완료부터 대표이미지 첫 요청까지 7,814.854ms가 비어 있다. 현재 소스의 `scheduleMdPickImagePreload()`는 8,000ms `setTimeout` 뒤 다시 `requestIdleCallback`으로 프리로드한다. 이번 실행 수치는 이 코드의 고정 지연과 직접 일치한다.

## 4. 상품 대표이미지

| 항목 | run03 |
|---|---:|
| 대표이미지 요청 | 14개 |
| 전송량 | 1,329,253 bytes |
| 첫 요청 시작 | 9,747ms |
| 첫 묶음 13개 완료 | 10,008ms |
| 개별 최대 소요 | 261ms |
| 데이터 준비 → 첫 요청 | 7,815ms |

14번째 이미지는 세 실행 모두 `goodSeq=30001242`였고 첫 묶음보다 각각 3.96초, 4.05초, 8.02초 늦게 시작했다. 이 이미지는 공통 프리로드 묶음과 다른 갱신 경로를 사용하는 후보로 분류한다.

## 5. 로그인 API

| 요청 | 시작 | 종료 | 소요 |
|---|---:|---:|---:|
| `home_bootstrap_light` | 1,709ms | 1,971ms | 262ms |
| 전체 새 일정 | 10,342ms | 11,454ms | 1,112ms |
| 내 새 일정 | 10,342ms | 10,832ms | 490ms |
| 내 참여 신청 | 11,511ms | 12,090ms | 579ms |
| 찜 조회 | 12,126ms | 12,738ms | 612ms |
| 홈 통계 | 12,760ms | 14,232ms | 1,472ms |

주요 공백:

- bootstrap 종료 → 일정 API 시작: 8,371ms
- 마지막 일정 API 종료 → 참여 API 시작: 57ms
- 참여 API 종료 → 찜 조회 시작: 36ms

run01·run02의 일정→참여 API 공백은 약 3.7~4.0초였지만 run03은 57ms다. 서버의 고정 대기라면 이렇게 사라질 수 없다. 두 일정 API의 응답 순서와 그 사이에 예약된 큰 렌더가 다음 요청 시작 시점을 바꾸는 구조로 판정한다.

## 6. 중복·실패 후보

- run01·run02에서 두 번씩 전송된 `taste_bg1~3`·국기 3종은 run03에서는 각각 한 번만 요청됐다.
- 따라서 골프조인 이미지 이중 전송은 항상 발생하는 중복이 아니라 렌더·프리로드 순서에 따른 조건부 중복이다.
- Naver 외부 스크립트 1종은 세 번 모두 두 번 요청됐다.
- Supabase `productCC1.jpg`는 세 번 모두 서버 연결 전에 blocked된 뒤 status 0으로 끝났다.

Supabase 이미지는 지속적인 실패 후보지만, 반복 DOM 교체로 요청이 취소된 것인지 실제 화면에서 깨진 것인지는 기능 검증으로 분리한다.

## 7. 파일 무결성과 개인정보

| 파일 | 크기 | SHA-256 |
|---|---:|---|
| raw HAR | 12,922,927 bytes | `16CBEEA3E9E80750EDBC2F899421BF35A0FE2749B2A2F612FA668278DB0F972B` |
| 개인정보 제거본 | 831,867 bytes | `51A4F5887B135E76E0A549F3AA3FD64DC45C6B464F8DF26BEB00B3B82A0BA37B` |
| 안전 분석본 | 19,581 bytes | `593DD583052BF4EDFE741F3EC831A4CD4B19B26B7381DE9D2740472124D565FF` |

- [x] raw HAR은 `.gitignore` 대상이다.
- [x] 제거본의 Cookie·본문은 제거되고 회원 쿼리값은 `[REDACTED]`다.
- [x] 안전 분석본의 회원 쿼리 이름·식별값·이메일·전화번호·Cookie는 모두 0건이다.
- [x] 제품 HTML은 수정하지 않았다.

## 8. 판정

- [x] run03을 공식 세 번째 표본으로 채택한다.
- [x] 대표이미지 요청 지연과 개별 다운로드 시간을 3회 기준선으로 확정한다.
- [x] 로그인 API 공백은 고정 서버 지연이 아닌 데이터 도착 순서 의존 문제로 분류한다.
- [x] 골프조인 이미지 이중 전송은 조건부 중복으로 분류한다.
- [x] Supabase 이미지 status 0은 별도 기능 검증 대상으로 넘긴다.
