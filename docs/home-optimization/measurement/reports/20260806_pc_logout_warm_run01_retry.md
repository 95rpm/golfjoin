# `8A853...` PC 비로그인 Warm 공식 run01

측정일: 2026-08-06 KST  
운영 URL: `https://www.secret-tour.com/event/plan_view?eventPlanSeq=3&page=1`  
운영 소스 경계: `8A853CA11B9DED569067D2E45317B883E44452868E7FA8E01A2CCFAD6A17ACCC`

## 1. 최종 판정

- [x] `Disable cache`가 꺼진 주소창 이동 측정이다.
- [x] 전체 121개 중 108개 요청이 0 bytes로 브라우저 캐시를 재사용했다.
- [x] 상품 대표이미지 14개는 모두 0 bytes다.
- [x] 버전 홈 카드 JSON은 0 bytes로 재사용됐다.
- [x] 비회원 앱 캐시가 재사용되어 골프조인 API는 `home_bootstrap_light` 1개만 실행됐다.
- [x] 공식 PC 비로그인 Warm run01로 채택한다.
- [ ] 같은 조건으로 run02·run03을 측정해 중앙값과 편차를 확정한다.

## 2. Cold 중앙값과 비교

| 지표 | PC 비로그인 Cold 중앙값 | Warm 공식 run01 | 변화 | 쉬운 설명 |
|---|---:|---:|---:|---|
| 요청 수 | 122개 | 121개 | -0.8% | 호출 개수는 거의 같지만 대부분 캐시에서 즉시 꺼냈다. |
| 총 전송량 | 5,997,156 bytes | 523,660 bytes | -91.3% | 실제 네트워크 다운로드가 약 9분의 1로 줄었다. |
| DOMContentLoaded | 1,716ms | 608ms | -64.6% | 기본 화면 구조가 준비되는 시간이 크게 줄었다. |
| Load | 10,012ms | 8,426ms | -15.8% | 전송량 감소에 비해 Load 개선은 작아 고정 지연 작업이 남아 있음을 보여준다. |
| 전체 이미지 전송량 | 3,983,725 bytes | 44 bytes | -99.999% | 44 bytes는 분석용 추적 이미지 2개의 합계다. |
| 상품 대표이미지 | 14개 / 1,329,351 bytes | 14개 / 0 bytes | -100% | 상품 사진은 모두 브라우저 캐시에서 재사용됐다. |
| 홈 카드 완료 | 1,796ms | 633ms | -64.7% | 화면 구성 데이터도 캐시에서 즉시 복원됐다. |
| 첫 상품이미지 요청 | 9,331ms | 3,957ms | -57.6% | 캐시가 있어도 요청 등록 자체는 약 4초 뒤다. |
| 마지막 상품이미지 요청 | 19,366ms | 15,840ms | -18.2% | 마지막 이미지는 여전히 15초 이후에야 화면 요청 목록에 들어온다. |
| 골프조인 API | 2개 | 1개 | -50% | `home_stats`는 앱 캐시에서 재사용됐다. |

핵심은 네트워크 전송량이 91.3% 줄었는데도 Load는 15.8%만 줄었다는 점이다. 브라우저가 사진과 스크립트를 이미 가지고 있어도 클라이언트 코드가 정해진 시간까지 기다린 뒤 렌더링하기 때문이다. Phase 2에서 고정 지연 타이머를 제거해야 사용자가 느끼는 속도가 전송량 감소만큼 개선된다.

## 3. HTTP 캐시 검증

| 자산 | 결과 | 실제 전송 | 판정 |
|---|---|---:|---|
| CSS 8개 | cache | 0 bytes | 정상 재사용 |
| JavaScript 44개 | cache | 0 bytes | 정상 재사용 |
| 이미지 54개 | 52개 0 bytes | 합계 44 bytes | 상품·UI 이미지 정상 재사용 |
| 홈 manifest | 304 | 13 bytes | revision 재검증 |
| 상품군 manifest | 304 | 13 bytes | revision 재검증 |
| 버전 홈 카드 JSON | status 200, cache | 0 bytes | 정상 재사용 |
| 상품군 catalog | 304 | 13 bytes | loader의 `max-age=0` 재검증 |

- [x] 전체 121개 중 108개가 0 bytes다.
- [x] `no-cache`는 5개뿐이며 동적 API·분석 요청·후속 마이페이지 확인 요청에 한정된다.
- [x] 이전 실패 표본의 `no-cache` 121개 현상은 사라졌다.
- [x] 본문을 다시 받은 정적 자산은 메인 HTML이 대부분이다.

메인 HTML은 490,847 bytes로 전체 Warm 전송량의 약 93.7%다. 캐시가 작동하는 재방문에서는 단일 HTML이 압도적인 전송 병목이므로 Phase 13의 CSS·JavaScript 외부 자산 분리와 HTML 경량화 효과를 비교할 수 있는 기준값이다.

## 4. 앱 캐시와 대표이미지 타이밍

실행된 골프조인 API:

- `home_bootstrap_light`: 568ms 시작, 170ms 소요, 738ms 완료

실행되지 않은 비회원 보조 API:

- `home_stats`

상품 대표이미지 요청은 모두 0 bytes였지만 요청 등록 시점은 다음처럼 늦게 남았다.

- 첫 2개: 약 3.96초
- 다음 11개: 약 7.08초
- `goodSeq=30001242`: 약 15.84초

7.08초 묶음과 마지막 이미지 사이의 약 8.76초 공백은 모바일 로그인 Warm 기준에서도 확인된 마지막 이미지 지연과 같은 계열이다. 이미지 서버가 아니라 프런트 타이머·렌더 순서가 원인이라는 판단을 강화한다.

## 5. 원홈페이지 요청

- `getEventTab.json`: 18ms
- `getEventGoodsList.json`: 62ms

두 요청은 합계 약 80ms이며 골프조인 대표이미지의 3.96~15.84초 요청 지연과 비교하면 병목이 아니다.

## 6. 파일 무결성과 개인정보

| 파일 | 크기 | SHA-256 |
|---|---:|---|
| raw HAR | 13,237,700 bytes | `0A0A220E8F5480DFBA0EF19A8F7BA94701714E63734BA2C5BA7327B56E7099A7` |
| 개인정보 제거본 | 581,859 bytes | `86E39521B56B770787EBDCAE5A6B5DC43EFB6B2FAB4D6E175EAAA79607B5A285` |
| 안전 분석본 | 18,951 bytes | `0A62AA6C48F5DBD4583471C7A497C66D6775205CD6AA850DDEAD66AE120C4B7F` |

- [x] raw HAR은 `.gitignore` 적용 대상이다.
- [x] 개인정보 제거본의 요청·응답 본문과 Cookie 값은 제거됐다.
- [x] 비회원 표본에 회원별 `memberKey`, `memberSeq`, `memberId`, `memberMobile`, `memberEmail` 요청은 없다.
- [x] 측정 도구 테스트 8/8을 통과했다.
- [x] 측정 전후 `golfjoin_main.html` SHA-256은 두 번 모두 `8A853...`다.
- [x] 제품 HTML은 수정하지 않았다.

## 7. 다음 단계

- [x] PC 비로그인 Warm 공식 run01 채택
- [ ] 동일한 로그아웃·PC·HTTP/앱 캐시 Warm 조건으로 공식 run02 측정
- [ ] 공식 run03 측정
- [ ] 3회 중앙값·편차와 Cold 대비 개선율 확정

