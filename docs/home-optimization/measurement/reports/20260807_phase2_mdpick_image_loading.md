# 2단계 2-1 MD PICK·취향맞춤 이미지 최적화 검증 보고서

- 작업일: 2026-08-07
- 대상 파일: `golfjoin_main.html`
- 현재 운영 HTML: `E6B5BC77E59339A93340BAC1B43DA68FB3C8EC6AC6C7BD35942C080103CE1575`
- 최종 로컬·운영 확인 HTML: `E6B5BC77E59339A93340BAC1B43DA68FB3C8EC6AC6C7BD35942C080103CE1575`
- 운영 데이터 구조 변경: 없음
- 서버·Cloud Function 변경: 없음
- 현재 판정: 운영 배포·사후검증 통과, 2-1 MD PICK·취향맞춤 이미지 최적화 완료

## 초보자를 위한 한 줄 설명

사진 다운로드가 느린 것이 아니라 브라우저가 사진을 요청하기 전에 불필요한 전체 목록 계산을 오래 하고 있었다. 상품과 상품군이 정확히 준비되는 즉시 대표 카드부터 그리고, 사진 요청 앞에 있던 전체 목록 계산을 제거했다.

## 이전 배포본 검증 결과

`D1BA1B9C...` 배포본에는 다음 1차 개선이 정상 반영됐다.

- [x] 중첩 `requestIdleCallback` 대기 제거
- [x] 고정 8초 이미지 프리로드 제거
- [x] 활성 MD PICK 이미지 `loading="eager"` 적용
- [x] 실제 히어로 이미지 하나에만 `fetchpriority="high"` 적용
- [x] `legacy-active-hidden` 중복 취향맞춤 카드 제거

그러나 운영에서 `products-ready` 이후 현재 대표이미지 요청까지 PC 약 4.90~5.20초, MO 약 4.64초가 걸려 100ms 목표를 충족하지 못했다. 따라서 `D1BA1B9C...`를 2-1 완료본으로 판정하지 않았다.

## 추가 진단으로 확인한 원인

운영 외곽 페이지와 공개 데이터를 그대로 사용하고 내부 GolfJoin HTML만 로컬 후보로 교체하는 사전검증을 만들었다.

첫 세부 측정에서 다음 결과를 확인했다.

| 구간 | 측정 예시 |
|---|---:|
| MD PICK 데이터 준비 | 2,891.2ms |
| 대표 카드 렌더 시작 | 2,891.9ms |
| 대표 카드 DOM 생성 완료 | 2,894.9ms |
| 실제 대표이미지 요청 시작 | 3,838.6ms |

카드 HTML 생성은 약 3ms였지만 DOM 생성 직후 `getHomeJoinSections()`가 전체 모임 목록을 다시 계산하면서 약 943ms 동안 이미지 요청을 막았다. 모바일 테스트는 `www`에서 `m`으로 이동한 뒤 운영 구버전을 측정할 수 있는 경계 문제도 발견해 PC와 MO 운영 URL을 분리했다.

## 최종 후보 적용 내용

- [x] 상품 값 준비와 전체 후처리 완료 Promise를 분리했다.
- [x] 상품·상품군이 성공 또는 실패로 실제 확정됐는지 상태를 직접 확인하고 MD PICK 렌더를 한 번만 예약한다.
- [x] MD PICK 단독 렌더에서는 `getHomeJoinSections()` 전체 계산을 실행하지 않는다.
- [x] MD PICK·취향맞춤 네비 칩만 갱신하는 경량 함수를 추가했다.
- [x] 취향맞춤은 1,200px 전 `IntersectionObserver` 경계에서 후속 렌더한다.
- [x] 활성 취향맞춤 이미지만 eager, 비활성 테마 이미지는 lazy로 유지한다.
- [x] 데이터 준비·대표 렌더 시작·DOM 완료를 개인정보 없는 정적 성능 마크로 기록한다.
- [x] 같은 성능 measure가 후속 재렌더 때 중복 생성되지 않게 했다.

`965F53BA...` PC 사전검증 예시에서 데이터 준비 후 실제 이미지 요청은 약 13.9ms, 홈 상품 준비 후 요청은 약 55.4ms였다.

## `965F53BA...` 운영 사후검증에서 추가 발견한 경합

PC 운영 공개 진단과 PC/MO 합성 로그인 진단은 통과했지만 MO 공개 진단에서 전체 홈 렌더가 상품 데이터보다 먼저 실행되는 순서를 확인했다. 이때 초기 정적 카드가 `primary-render-start`와 `primary-dom-ready`를 먼저 기록해 `data-ready → DOM` measure가 만들어지지 않았다.

다음 보정을 추가했다.

- [x] 초기 정적 카드 렌더에서는 최종 MD PICK 성능 마크와 첫 이미지 관찰을 시작하지 않는다.
- [x] `data-ready` 이후 대표 카드 렌더에서만 시작·DOM·첫 이미지 마크를 기록한다.
- [x] 상품·상품군 확정 직후 약 3ms의 대표 카드 렌더를 동기 실행해 긴 후처리보다 이미지 요청을 먼저 시작한다.
- [x] 초기 로딩 때문에 즉시 렌더할 수 없을 때만 기존 다음 프레임 예약을 fallback으로 사용한다.
- [x] 최종 카드 이미지가 `data-ready` 전에 이미 요청됐으면 지연을 0ms로 판정하고, 후속 중복 요청은 성능 기준에서 제외한다.

보정 전 편차 회차는 `data-ready → 렌더 시작`이 4,088.7ms, 대표 카드 렌더가 3.3ms, DOM 완료 → 이미지 요청이 -0.4ms였다. 즉 병목은 카드 생성이 아니라 예약된 렌더 앞의 긴 메인 스레드 작업이었다. `E0DFF60E...`는 즉시 렌더 보정 후 PC 3회·MO 3회 100ms 조건을 모두 통과했다.

## 리스크 제한

- 상품, 상품군, 일정, 회원, 참여자 데이터의 선택·병합 규칙을 변경하지 않았다.
- 이미지 URL과 오류 fallback 경로를 변경하지 않았다.
- 기존 전체 홈 후처리 Promise와 전체 홈 렌더 경로는 유지했다.
- 전체 섹션 네비 갱신은 기존 전체 렌더에서 유지하고, MD PICK 단독 렌더만 경량 갱신을 사용한다.
- 실제 회원 계정이나 운영 쓰기 API를 테스트에 사용하지 않았다.

## 최종 후보 자동검사 결과

| 검사 | 결과 |
|---|---:|
| 인라인 JavaScript 문법 | 2/2 |
| 단위검사 | 10/10 |
| PC/MO 100ms 반복검사 | 6/6 |
| PC/MO 운영 셸 성능·개인정보 진단 | 6/6 |
| 기본·스크롤·상세 응답 경합·상품군 기간 | 8/8 |
| 공개 데이터 장애 fallback | 10/10 |
| A 생성·B 참여·모집완료·취소 | 8/8 |
| 전체 회원 상태·딥링크·캐시 | 20/20 |
| 직접 `console.warn/error/log` 호출 | 0건 |
| 임시 후보 진단 로그 | 0건 |
| `git diff --check` | 통과 |

## `E0DFF60E...` 운영 사후검증 결과

- [x] PC와 MO 운영 응답에서 즉시 대표 카드 렌더, `data-ready` 보호, 경량 네비 갱신, 1,200px 취향맞춤 지연 렌더 코드를 확인했다.
- [x] 운영 공개 페이지의 부팅·첫 이미지·상품상세와 합성 로그인 개인화·마스킹 진단을 PC/MO 합계 4/4 통과했다.
- [x] 공개 페이지에서 데이터 준비 후 최종 대표이미지 요청 100ms 이내 조건을 PC 3/3·MO 3/3, 합계 6/6 통과했다.
- [x] 국가·팩·취향맞춤 전환과 기본 상품상세 회귀를 운영 진단에서 확인했다.

운영 검증은 실제 회원 개인정보나 쓰기 API를 사용하지 않았다. 로그인 경로는 합성 회원 응답만 사용하고 알 수 없는 쓰기 요청을 차단했다.

네트워크 권한 없이 처음 병렬 실행한 회귀 묶음은 모든 시나리오가 `net::ERR_NETWORK_ACCESS_DENIED`로 페이지 진입 전에 종료돼 기능 결과에서 제외했다. 같은 테스트를 읽기 전용 네트워크 권한으로 다시 실행한 위 결과만 최종 판정에 사용했다.

## 운영 배포 체크리스트

- [x] `D1BA1B9C...` 1차 HTML 운영 배포
- [x] `D1BA1B9C...` 운영 100ms 목표 미달 확인
- [x] `965F53BA...` 최종 후보 PC/MO 운영 셸 사전검증
- [x] `965F53BA...` HTML 운영 배포와 PC/MO 소스 반영 확인
- [x] `965F53BA...` PC 운영 공개·PC/MO 합성 로그인 진단 통과
- [x] `965F53BA...` MO 공개 진단의 초기 정적 성능 마크 경합 확인
- [x] `E0DFF60E...` PC 3회·MO 3회 100ms 반복검사와 전체 진단 6/6 통과
- [x] `E0DFF60E...` HTML 운영 배포
- [x] PC/MO 운영 응답에 즉시 대표 카드 렌더 코드 포함 확인
- [x] PC 운영 데이터 준비 후 첫 이미지 요청 100ms 이내 확인 — 3/3 통과
- [x] MO 운영 데이터 준비 후 첫 이미지 요청 100ms 이내 확인 — 3/3 통과
- [x] 운영 국가·팩·취향맞춤 전환과 기본 상세 회귀 확인

대표 카드·첫 이미지 즉시 요청 단위를 완료한 뒤 아래 후속 최적화도 운영에 반영하고 PC/MO 사후검증을 통과했다.

## `E6B5BC77...` 후속 배포 후보

- 전체 SHA-256: `E6B5BC77E59339A93340BAC1B43DA68FB3C8EC6AC6C7BD35942C080103CE1575`
- [x] 취향맞춤 최초 동적 DOM은 활성 테마 상품 3장만 만든다.
- [x] 비활성 테마 상품 카드와 배경 이미지는 전환·모바일 접근 시 생성한다.
- [x] 국가·골프팩/항공팩·테마 hover, focus, touch 의도에서만 선행 요청한다.
- [x] 선행 요청은 `fetchPriority="low"`, 동시 최대 2장으로 제한한다.
- [x] `saveData`, `slow-2g`, `2g`에서는 선택 전 선행 요청을 차단한다.
- [x] MD PICK과 취향맞춤의 깨진 이미지에 연회색 공통 fallback을 표시한다.

### 후보 자동검증

| 검사 | 결과 |
|---|---:|
| JavaScript 문법 | 2/2 |
| 단위검사 | 12/12 |
| 후보 운영 셸 핵심 PC/MO | 2/2 |
| 부팅·이미지·상세·개인정보 전체 진단 | 6/6 |
| 공개 장애 fallback | 10/10 |
| 전체 회원·딥링크·캐시 | 20/20 |
| 기본 메인·상세·스크롤·상품군 기간 | 7/8 + 실패 1건 단독 통과 |
| A 생성·B 참여·모집완료·취소 | 7/8 + 실패 1건 단독 통과 |
| 직접 `console.warn/error/log` | 0건 |
| 임시 운영 진단 식별자 | 0건 |
| `git diff --check` | 통과 |

기본 묶음의 1건은 PC 상품군 기간 데이터가 60초 안에 오지 않아 버튼 0개로 종료됐으나 같은 코드·상품의 단독 재검사는 52.2초에 통과했다. 참여자 묶음의 1건은 모바일 운영 HTML 본문 전송 중 `ECONNRESET`으로 후보 코드 실행 전에 종료됐으며 단독 재검사는 7.1초에 통과했다. 두 건 모두 기능 불일치로 판정하지 않았지만 운영 응답 편차 기록은 유지한다.

### 후속 후보 배포 체크리스트

- [x] `E6B5BC77...` HTML 운영 배포
- [x] PC/MO 운영 응답에 interaction prefetch·TOP3 지연 생성 코드 포함 확인
- [x] PC/MO 최초 취향맞춤 동적 DOM 3장 확인
- [x] 국가·팩·테마 전환 후 이미지와 카드 정상 표시
- [x] PC/MO 깨진 이미지 fallback 운영 페이지 재검증
- [x] 공개 부팅·첫 이미지·상품상세 성능 회귀 확인 — 전체 진단 4/4 통과
- [x] 공개 페이지 반복검사 — PC 3/3·MO 3/3 통과

`E6B5BC77...` 운영 사후검증에서 모든 필수 항목을 통과해 2-1을 완료했다. 검사는 실제 회원 개인정보와 운영 쓰기 API를 사용하지 않았으며, 깨진 이미지 검사는 브라우저의 로컬 DOM만 임시 변경했다.
