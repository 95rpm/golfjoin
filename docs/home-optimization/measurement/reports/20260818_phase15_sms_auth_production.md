# 15단계 일반회원 SMS 인증 운영 결과

## 결론

- [x] 운영 HTML `1AD8D64C` 배포 및 최종 검증 완료
- [x] 모든 HOME 일반회원 로그인에 SMS 인증 적용
- [x] 신규 일반회원 가입 휴대폰 인증 적용
- [x] 카카오 회원은 SMS 인증에서 제외
- [x] 인증 성공 세션 24시간 유지
- [x] 로그인·가입 인증번호 유효시간 3분 적용
- [x] 비공개 인증 버킷과 공개 데이터 저장소 분리
- [x] 운영 최종 회귀에서 신규 콘솔 오류 없음

## 운영에서 확인한 흐름

- [x] PC·모바일 비로그인 메인, 상품상세, 스크롤과 복원 정상
- [x] 카카오 회원 로그인, 나의 모임, 내예약, 참여자 정보 정상이며 `member_auth` 요청 없음
- [x] 일반회원 로그인 시 `member_auth_start` 1회·HTTP 202, 문자 1회 수신
- [x] 마스킹 휴대폰 번호, 3분 타이머, 인증 UI 정상
- [x] 인증번호 검증 후 메인 진입 정상
- [x] 새로고침 후 24시간 인증 세션 유지, 추가 문자 없음
- [x] 빈 `memberSeq` 일반회원도 ERP 조회로 회원번호를 확정해 정상 인증
- [x] 신규가입 시 Google Sheet 프로필 생성과 ERP 회원가입 완료
- [x] 신규가입 직후 ERP 반영 지연을 재조회로 흡수
- [x] 로그아웃 후 재로그인에서 회원정보 불일치 문구 없이 재인증 정상
- [x] 인증번호 3분 만료 후 입력창 타이머가 `재전송` 버튼으로 전환
- [x] 재전송 문자 1회 수신, 새 3분 타이머 시작, 새 인증번호 로그인 정상
- [x] 운영 `1AD8D64C`에서 일반회원 24시간 유지·카카오 제외·비로그인 회귀 최종 정상

## 복구 기준

- [x] 화면 문제: `ROLLBACK_golfjoin_main_9FA1AF4C.html` 재배포
- [x] 개인 API 보호 문제: `GOLFJOIN_MEMBER_AUTH_GATE=off` 유지
- [x] 인증 API 장애: `GOLFJOIN_MEMBER_AUTH_ENABLED=N`으로 중지
- [x] 현재 인증 서버 revision: `golfjoin-sheet-api-00214-rim`
- [x] 화면 복구 기준 운영본: `9FA1AF4C`

## 다음 단계로 넘기는 검증

아래 항목은 정상 운영 경로 완료와 구분한다. 사용자에게 고의로 실패를 만들거나 운영 트래픽을 이전 revision으로 돌려야 하므로 자동으로 완료 처리하지 않는다.

- [x] 틀린 인증번호 1회가 HTTP 401 `member_otp_invalid`로 거부되고 `attemptsRemaining: 4`를 반환하는지 확인 — eventPlanSeq 28에서 통과
- [x] 동일 인증 요청에서 이어서 올바른 인증번호를 입력하면 HTTP 200으로 정상 인증되는지 확인 — eventPlanSeq 28에서 통과
- [x] 틀린 인증번호를 누적 5회 입력할 경우 잠금과 안내 확인 — eventPlanSeq 28에서 통과
- [x] 인증번호 3분 만료 후 재전송 전환 확인 — eventPlanSeq 28에서 통과
- [x] 재전송 후 이전 인증번호가 거부되는지 확인 — eventPlanSeq 28에서 이전 번호 HTTP 401 거부, 새 번호 HTTP 200 인증 통과
- [x] 신규가입 중 휴대폰 번호 변경 시 기존 인증 증명 무효 확인 — eventPlanSeq 28에서 번호 변경 즉시 인증완료 상태 제거와 원번호 복원 시 자동복원 방지를 확인
- [x] 운영 Gate OFF 재확인과 화면 복구본 전환 후 원복 시험 — 함수 `ACTIVE`, Gate `off` 확인 후 eventPlanSeq 28에서 `9FA1AF4C` 복구 및 `1AD8D64C` 원복 통과
- [x] 카카오 회원용 서버 신원 토큰 경로 마련 전 `enforce` 금지 유지 — 운영 Gate `off` 재확인

## 28번 재전송 시험에서 발견한 보완

- [x] 기존 하단 `인증번호 다시 받기` 텍스트가 인증 만료 전부터 노출되는 문제를 확인했다.
- [x] 반복 시험으로 회원별 시간당 5회 제한에 도달했을 때 `1531초 후`처럼 긴 서버 제한 시간이 그대로 표시되는 문제를 확인했다.
- [x] 인증번호 유효시간 3분 동안 입력창 오른쪽에 타이머만 표시하도록 변경했다.
- [x] 3분 만료 후 같은 위치의 타이머를 `재전송` 버튼으로 교체하도록 변경했다.
- [x] 재전송 성공 시 새 challenge와 새 3분 타이머를 사용하고 이전 인증번호를 폐기하는 기존 서버 구조를 유지했다.
- [x] 정상적인 만료·재전송 시험을 허용하면서 남용 방지 상한을 유지하도록 회원별 시간당 제한을 5회에서 10회로 조정했다.
- [x] 5분 이상 남은 제한은 초 단위 숫자 대신 요청 횟수가 많다는 안내로 표시하도록 변경했다.
- [x] 현재 운영 `AFCF6114`를 정확한 복구본으로 포함한 v15 패키지를 만들었다.
- [x] 전용 42/42와 서버 전체 182/182 시험을 통과했다.
- [x] Cloud Shell 배포 전 서버 전체 261건에서 실패 0건을 확인했다(통과 240, 의도적 제외 21).
- [x] Cloud Function `golfjoin-sheet-api-00214-rim`이 `ACTIVE`인 것을 확인했다.
- [x] GCS 자산 revision `gha_1a7c3740af867e49ae4bba56`의 gzip CSS와 Brotli JS 응답을 확인했다.
- [x] eventPlanSeq 28 전체 회귀와 운영 `67E4438D` 최종 회귀를 모두 통과했다.

## 6자리 버튼·잠금 복구 보완

- [x] 로그인 인증번호가 숫자 6자리일 때만 `인증하기` 버튼이 활성화된다.
- [x] 0~5자리에서는 연한 회색 비활성 상태, 6자리에서는 파란 활성 상태로 표시된다.
- [x] 한 자리를 삭제하면 버튼이 즉시 다시 비활성화된다.
- [x] eventPlanSeq 28에서 잘못된 인증번호 5회 입력 후 `member_otp_locked` 잠금을 확인했다.
- [x] 잠금 뒤 재전송 제한시간 안내와 제한시간 경과 후 새 문자 발송을 확인했다.
- [x] 새 인증번호로 정상 로그인하고 콘솔 오류가 없음을 확인했다.
- [x] 운영 전환 후보 `9FA1AF4C`, 즉시 복구본 `67E4438D`, 자산 revision `gha_423af7fac7b620059d678464`로 고정했다.
- [x] 운영 `9FA1AF4C` 배포 후 일반회원 버튼 상태·로그인, 나의 모임·내예약·상품상세, 카카오 OTP 제외와 콘솔 오류 0건을 최종 확인했다.

## 회원가입 인증완료 번호 잠금 보완

- [x] 인증 완료 후 휴대폰 번호 입력창을 비활성화해 번호를 다시 수정할 수 없게 했다.
- [x] 인증 완료 시 발송 버튼과 인증번호 입력 영역을 숨기고 `인증완료` 배지만 유지한다.
- [x] eventPlanSeq 28에서 PC·모바일 입력 차단, 다음 단계 활성화와 번호 가독성을 확인했다.
- [x] eventPlanSeq 28에서 비로그인·카카오·일반회원 전체 회귀와 콘솔 오류 0건을 확인했다.
- [x] 후보 `1AD8D64C`, 즉시 복구본 `9FA1AF4C`, 자산 revision `gha_ef8e352403c11a7a33d37594`로 고정했다.
- [x] 운영 `1AD8D64C` 배포 후 번호 잠금, 비로그인·카카오·일반회원 회귀와 콘솔 오류 0건을 최종 확인했다.

## 마이메뉴 포커스 접근성 보완

- [x] 카카오 회원이 마이메뉴에서 내예약으로 이동할 때 포커스된 버튼의 조상에 `aria-hidden`이 적용되던 경고를 재현했다.
- [x] 닫힌 마이메뉴에 `inert`를 함께 적용하고, 닫기 전에 내부 포커스를 해제하도록 수정했다.
- [x] 마이메뉴를 열 때 `inert`를 먼저 해제하고 `aria-hidden="false"`를 적용하도록 순서를 고정했다.
- [x] 접근성 집중 시험 11/11과 프런트엔드 전체 시험 160건에서 실패 0건을 확인했다(통과 157, 조건부 제외 3).
- [x] eventPlanSeq 28에서 닫힘 상태 `aria-hidden=true`·`inert=true`·내부 포커스 없음, 열림 상태 `aria-hidden=false`·`inert=false`·`open=true`를 확인했다.
- [x] 후보 `C8F39986`, 즉시 복구본 `1AD8D64C`, 자산 revision `gha_b54ed5c7407d55880f189a10`으로 고정했다.
- [x] 운영 `C8F39986` 배포 후 카카오 내예약 진입에서 기존 `Blocked aria-hidden` 경고가 사라지고 Error·Warning 0건임을 확인했다.

## 2026-08-21 회원 인증 Enforce 최종 완료

이 문서 앞부분의 Gate `off`와 카카오 회원 토큰 부재는 2026-08-18 당시 상태다. 이후 다음 보완과 운영 전환을 완료했다.

- [x] 카카오 공식 access token을 서버에서 검증하고 Secret Tour 외부회원 ID와 일치할 때 GolfJoin 회원 세션으로 교환했다.
- [x] 기존 HOME 일반회원의 빈 memberSeq 프로필을 검증된 memberId alias로 안전하게 연결했다.
- [x] 기존 일반회원 추가정보 화면 깜빡임을 제거하고 실제 미완료 회원만 입력 화면을 보이게 했다.
- [x] HMAC Report에서 일반회원·카카오회원 개인 읽기와 찜 쓰기의 `missing`·`invalid`·`mismatch` 0건을 확인했다.
- [x] Gate `enforce`의 무토큰 보호 요청이 HTTP 401 `member_token_required`로 거부되는 것을 확인했다.
- [x] 일반회원·카카오회원·비로그인, 나의 모임·내예약·찜·상품상세 운영 회귀를 통과했다.
- [x] Report revision `golfjoin-sheet-api-00230-qid` 복구와 Enforce revision `golfjoin-sheet-api-00231-yuz` 원복을 완료했다.
- [x] 최종 운영 HTML `57A4BEC1`, 자산 `gha_3075b235e554d023f9db2336`, 화면 복구 `7502BFEA`를 확정했다.

최종 인수와 복구 기준은 `20260821_optimization_security_final_handoff.md`에서 관리한다.

## 8R 회원 인증 Report 운영 관찰 — 2026-08-18 당시 기록

- [x] HMAC 익명 참조 보완 서버 `golfjoin-sheet-api-00215-vux`를 Gate `off`로 먼저 검증했다.
- [x] Report 관찰 리비전 `golfjoin-sheet-api-00216-foq`를 `ACTIVE`, 인증 `Y`, Gate `report`로 확인했다.
- [x] 카카오 회원과 일반회원에서 메인·나의 모임·내예약 읽기 동작을 수행했으며 화면 오류가 없었다.
- [x] 총 13건은 `match` 7건·`missing` 6건·`mismatch` 0건·`invalid` 0건이었다.
- [x] 일반회원 `match` 로그는 요청·검증 HMAC 참조값이 동일했고, 실제 회원번호·이름·휴대폰·이메일·인증번호·토큰은 기록되지 않았다.
- [x] 당시 카카오 회원과 `write:join_member_profile` 초기 요청에 토큰이 없는 것을 확인해 그 리비전의 `enforce` 전환을 보류했다. 이후 카카오 공식 토큰 교환과 쓰기 인증을 보완해 2026-08-21 Enforce 전환을 완료했다.
- [x] 관찰 종료 후 `golfjoin-sheet-api-00217-quw`, `ACTIVE`, 인증 `Y`, Gate `off`로 복구했다.
